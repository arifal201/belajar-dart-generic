class NumberData<T extends num> {
  /**
   * belajar membuat bounded data type dart
   * untuk membatasi tipe data yang digunakan kita dapat membatasi dengan kata kunci extends lalu diikuti tipe data 
   */
  
  T data;

  NumberData(this.data);
}
